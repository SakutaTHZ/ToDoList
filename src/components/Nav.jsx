import React from 'react'

const Nav = () => {
  return (
    <nav>
        <span className="material-symbols-outlined">menu</span>
        <span className='title'>List Tracker</span>
        <span className="material-symbols-outlined">download_done</span>
        </nav>
  )
}

export default Nav